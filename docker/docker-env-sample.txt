# Docker-specific environment overrides for Flexible GraphRAG
# This file overrides localhost addresses with Docker service names
# Copy this to docker/docker.env and customize as needed:
#   Windows: copy docker\docker-env-sample.txt docker\docker.env
#   macOS/Linux: cp docker/docker-env-sample.txt docker/docker.env
#
# The main flexible-graphrag/.env file is loaded first, then docker.env
# overrides only the Docker networking configuration.
#
# Use per-store config vars only ({TYPE}_GRAPH_DB_CONFIG, {TYPE}_VECTOR_DB_CONFIG,
# {TYPE}_SEARCH_DB_CONFIG). These match PG_GRAPH_DB / VECTOR_DB / SEARCH_DB in .env.

# ====================================================================
# DOCKER DEPLOYMENT SCENARIOS
# ====================================================================
# SCENARIO A: Databases in Docker, app on host (hybrid) - RECOMMENDED FOR DEV
#   - Use host.docker.internal in the per-store configs below
#   - Best for: development, hot-reload, debugging
#
# SCENARIO B: Full stack in Docker
#   - Use Docker Compose service names (neo4j, qdrant, elasticsearch, ...)
#   - Backend + UI containers talk to DBs on the internal network
#   - Exception: Ollama usually stays on the host -> host.docker.internal
# ====================================================================

# ====================================================================
# Property graph databases ({TYPE}_GRAPH_DB_CONFIG)
# ====================================================================

# Neo4j (active when PG_GRAPH_DB=neo4j)
# SCENARIO A:
NEO4J_GRAPH_DB_CONFIG={"url": "bolt://host.docker.internal:7687", "username": "neo4j", "password": "password"}
# SCENARIO B: #NEO4J_GRAPH_DB_CONFIG={"url": "bolt://neo4j:7687", "username": "neo4j", "password": "password"}

# FalkorDB (active when PG_GRAPH_DB=falkordb)
# SCENARIO A: #FALKORDB_GRAPH_DB_CONFIG={"url": "falkor://host.docker.internal:6379", "database": "falkor"}
# SCENARIO B: #FALKORDB_GRAPH_DB_CONFIG={"url": "falkor://falkordb:6379", "database": "falkor"}

# ArcadeDB (active when PG_GRAPH_DB=arcadedb)
# SCENARIO A: #ARCADEDB_GRAPH_DB_CONFIG={"host": "host.docker.internal", "port": 2480, "username": "root", "password": "playwithdata", "database": "flexible_graphrag", "include_basic_schema": true}
# SCENARIO B: #ARCADEDB_GRAPH_DB_CONFIG={"host": "arcadedb", "port": 2480, "username": "root", "password": "playwithdata", "database": "flexible_graphrag", "include_basic_schema": true}

# MemGraph (active when PG_GRAPH_DB=memgraph)
# SCENARIO A: #MEMGRAPH_GRAPH_DB_CONFIG={"url": "bolt://host.docker.internal:7688", "username": "", "password": "", "database": "memgraph"}
# SCENARIO B: #MEMGRAPH_GRAPH_DB_CONFIG={"url": "bolt://memgraph:7687", "username": "", "password": "", "database": "memgraph"}

# NebulaGraph (active when PG_GRAPH_DB=nebula)
# SCENARIO A: #NEBULA_GRAPH_DB_CONFIG={"space": "flexible_graphrag", "overwrite": true, "address": "host.docker.internal", "port": 9669, "username": "root", "password": "nebula"}
# SCENARIO B: #NEBULA_GRAPH_DB_CONFIG={"space": "flexible_graphrag", "overwrite": true, "address": "nebula-graphd", "port": 9669, "username": "root", "password": "nebula"}

# ArangoDB (active when PG_GRAPH_DB=arangodb)
# SCENARIO A: #ARANGODB_GRAPH_DB_CONFIG={"url": "http://host.docker.internal:8529", "database": "flexible-graphrag", "username": "root", "password": "testpass", "graph_name": "knowledge_graph"}
# SCENARIO B: #ARANGODB_GRAPH_DB_CONFIG={"url": "http://arangodb:8529", "database": "flexible-graphrag", "username": "root", "password": "testpass", "graph_name": "knowledge_graph"}

# Apache AGE (active when PG_GRAPH_DB=apache_age)
# SCENARIO A: #APACHE_AGE_GRAPH_DB_CONFIG={"host": "host.docker.internal", "port": 5434, "database": "flexible_graphrag_age", "username": "postgres", "password": "password", "graph_name": "knowledge_graph", "collection_name": "langchain_age_vectors", "search_type": "hybrid"}
# SCENARIO B: #APACHE_AGE_GRAPH_DB_CONFIG={"host": "apache-age", "port": 5432, "database": "flexible_graphrag_age", "username": "postgres", "password": "password", "graph_name": "knowledge_graph", "collection_name": "langchain_age_vectors", "search_type": "hybrid"}

# Cosmos Gremlin (active when PG_GRAPH_DB=cosmos_gremlin)
# SCENARIO A: #COSMOS_GREMLIN_GRAPH_DB_CONFIG={"url": "ws://host.docker.internal:8182/gremlin", "username": "/", "password": ""}
# SCENARIO B: #COSMOS_GREMLIN_GRAPH_DB_CONFIG={"url": "ws://gremlin-server:8182/gremlin", "username": "/", "password": ""}

# HugeGraph (active when PG_GRAPH_DB=hugegraph)
# SCENARIO A: #HUGEGRAPH_GRAPH_DB_CONFIG={"host": "host.docker.internal", "port": 8082, "username": "admin", "password": "password", "database": "hugegraph"}
# SCENARIO B: #HUGEGRAPH_GRAPH_DB_CONFIG={"host": "hugegraph", "port": 8080, "username": "admin", "password": "password", "database": "hugegraph"}

# SurrealDB (active when PG_GRAPH_DB=surrealdb)
# SCENARIO A: #SURREALDB_GRAPH_DB_CONFIG={"url": "ws://host.docker.internal:8010/rpc", "namespace": "test", "database": "flexible_graphrag", "username": "root", "password": "root"}
# SCENARIO B: #SURREALDB_GRAPH_DB_CONFIG={"url": "ws://surrealdb:8000/rpc", "namespace": "test", "database": "flexible_graphrag", "username": "root", "password": "root"}

# TigerGraph (active when PG_GRAPH_DB=tigergraph)
# SCENARIO A: #TIGERGRAPH_GRAPH_DB_CONFIG={"host": "http://host.docker.internal", "port": 14240, "restpp_port": 9002, "database": "MyGraph", "username": "tigergraph", "password": "tigergraph"}
# SCENARIO B: #TIGERGRAPH_GRAPH_DB_CONFIG={"host": "http://tigergraph", "port": 14240, "restpp_port": 9000, "database": "MyGraph", "username": "tigergraph", "password": "tigergraph"}

# Ladybug / Kuzu / LanceDB: file paths only -- use volume mounts, no network override

# Neptune / Neptune Analytics / Spanner: configured in main .env (cloud endpoints)

# ====================================================================
# Vector databases ({TYPE}_VECTOR_DB_CONFIG)
# ====================================================================

# Qdrant (active when VECTOR_DB=qdrant)
# SCENARIO A:
QDRANT_VECTOR_DB_CONFIG={"host": "host.docker.internal", "port": 6333, "collection_name": "hybrid_search_vector", "https": false}
# SCENARIO B: #QDRANT_VECTOR_DB_CONFIG={"host": "qdrant", "port": 6333, "collection_name": "hybrid_search_vector", "https": false}

# Elasticsearch vector (active when VECTOR_DB=elasticsearch)
# SCENARIO A: #ELASTICSEARCH_VECTOR_DB_CONFIG={"url": "http://host.docker.internal:9200", "index_name": "hybrid_search_vector"}
# SCENARIO B: #ELASTICSEARCH_VECTOR_DB_CONFIG={"url": "http://elasticsearch:9200", "index_name": "hybrid_search_vector"}

# OpenSearch vector (active when VECTOR_DB=opensearch)
# SCENARIO A: #OPENSEARCH_VECTOR_DB_CONFIG={"url": "http://host.docker.internal:9201", "index_name": "hybrid_search_vector"}
# SCENARIO B: #OPENSEARCH_VECTOR_DB_CONFIG={"url": "http://opensearch:9200", "index_name": "hybrid_search_vector"}

# Neo4j vector (active when VECTOR_DB=neo4j)
# SCENARIO A: #NEO4J_VECTOR_DB_CONFIG={"url": "bolt://host.docker.internal:7687", "username": "neo4j", "password": "password", "index_name": "hybrid_search_vector", "database": "neo4j"}
# SCENARIO B: #NEO4J_VECTOR_DB_CONFIG={"url": "bolt://neo4j:7687", "username": "neo4j", "password": "password", "index_name": "hybrid_search_vector", "database": "neo4j"}

# Chroma (active when VECTOR_DB=chroma)
# SCENARIO A: #CHROMA_VECTOR_DB_CONFIG={"host": "host.docker.internal", "port": 8001, "collection_name": "hybrid_search"}
# SCENARIO B: #CHROMA_VECTOR_DB_CONFIG={"host": "chroma", "port": 8001, "collection_name": "hybrid_search"}

# Milvus (active when VECTOR_DB=milvus)
# SCENARIO A: #MILVUS_VECTOR_DB_CONFIG={"host": "host.docker.internal", "port": 19530, "collection_name": "hybrid_search", "username": "root", "password": "milvus"}
# SCENARIO B: #MILVUS_VECTOR_DB_CONFIG={"host": "milvus-standalone", "port": 19530, "collection_name": "hybrid_search", "username": "root", "password": "milvus"}

# Weaviate (active when VECTOR_DB=weaviate)
# SCENARIO A: #WEAVIATE_VECTOR_DB_CONFIG={"url": "http://host.docker.internal:8081", "index_name": "HybridSearch"}
# SCENARIO B: #WEAVIATE_VECTOR_DB_CONFIG={"url": "http://weaviate:8080", "index_name": "HybridSearch"}

# PostgreSQL pgvector (active when VECTOR_DB=postgres)
# SCENARIO A: #POSTGRES_VECTOR_DB_CONFIG={"host": "host.docker.internal", "port": 5433, "database": "postgres", "username": "postgres", "password": "password", "table_name": "hybrid_search_vectors"}
# SCENARIO B: #POSTGRES_VECTOR_DB_CONFIG={"host": "postgres-pgvector", "port": 5432, "database": "postgres", "username": "postgres", "password": "password", "table_name": "hybrid_search_vectors"}

# LanceDB: local path only -- use volume mount
# Pinecone: cloud -- configured in main .env

# ====================================================================
# Search databases ({TYPE}_SEARCH_DB_CONFIG)
# ====================================================================

# Elasticsearch search (active when SEARCH_DB=elasticsearch)
# SCENARIO A:
ELASTICSEARCH_SEARCH_DB_CONFIG={"url": "http://host.docker.internal:9200", "index_name": "hybrid_search_fulltext"}
# SCENARIO B: #ELASTICSEARCH_SEARCH_DB_CONFIG={"url": "http://elasticsearch:9200", "index_name": "hybrid_search_fulltext"}

# OpenSearch search (active when SEARCH_DB=opensearch)
# SCENARIO A: #OPENSEARCH_SEARCH_DB_CONFIG={"url": "http://host.docker.internal:9201", "index_name": "hybrid_search_fulltext"}
# SCENARIO B: #OPENSEARCH_SEARCH_DB_CONFIG={"url": "http://opensearch:9200", "index_name": "hybrid_search_fulltext"}

# ====================================================================
# Ontology paths (USE_ONTOLOGY=true)
# ====================================================================
# Repo schemas/ is mounted at /app/schemas in the backend container (app-stack.yaml).
# ../schemas/ from .env resolves outside the container and fails in Docker.
# SCENARIO A (hybrid, app on host): keep ../schemas/ in flexible-graphrag/.env — no override needed
# SCENARIO B (full stack in Docker): #ONTOLOGY_DIR=schemas/

# ====================================================================
# Incremental updates (ENABLE_INCREMENTAL_UPDATES=true)
# ====================================================================
# postgres-pgvector listens on 5432 inside Docker; host maps 5433->5432.
# SCENARIO A:
POSTGRES_INCREMENTAL_URL=postgresql://postgres:password@host.docker.internal:5433/flexible_graphrag_incremental
# SCENARIO B: #POSTGRES_INCREMENTAL_URL=postgresql://postgres:password@postgres-pgvector:5432/flexible_graphrag_incremental

# ====================================================================
# RDF graph stores (individual prefixed vars -- no JSON blob yet)
# ====================================================================

# Fuseki (active when RDF_GRAPH_DB=fuseki)
# SCENARIO A: #FUSEKI_BASE_URL=http://host.docker.internal:3030
# SCENARIO B: #FUSEKI_BASE_URL=http://fuseki:3030

# GraphDB (active when RDF_GRAPH_DB=graphdb)
# SCENARIO A: #GRAPHDB_BASE_URL=http://host.docker.internal:7200
# SCENARIO B: #GRAPHDB_BASE_URL=http://graphdb:7200

# Oxigraph (active when RDF_GRAPH_DB=oxigraph)
# SCENARIO A: #OXIGRAPH_URL=http://host.docker.internal:7878
# SCENARIO B: #OXIGRAPH_URL=http://oxigraph:7878

# ====================================================================
# Content sources
# ====================================================================

# Alfresco / CMIS (when using docker/includes/alfresco.yaml)
# SCENARIO A: #ALFRESCO_CONFIG={"base_url": "http://host.docker.internal:8080/alfresco", "username": "admin", "password": "admin"}
# SCENARIO B: #ALFRESCO_CONFIG={"base_url": "http://alfresco:8080/alfresco", "username": "admin", "password": "admin"}
# SCENARIO A: #CMIS_CONFIG={"url": "http://host.docker.internal:8080/alfresco/api/-default-/public/cmis/versions/1.1/atom", "username": "admin", "password": "admin", "repository_id": "-default-"}
# SCENARIO B: #CMIS_CONFIG={"url": "http://alfresco:8080/alfresco/api/-default-/public/cmis/versions/1.1/atom", "username": "admin", "password": "admin", "repository_id": "-default-"}

# ====================================================================
# LLM providers (host vs container networking)
# ====================================================================

# Ollama on host, backend in Docker
OLLAMA_BASE_URL=http://host.docker.internal:11434
# Ollama in Docker (uncommon): #OLLAMA_BASE_URL=http://ollama:11434

# vLLM in Docker (see docker/includes/vllm.yaml)
#VLLM_API_BASE=http://vllm:8000/v1
# Standalone backend on host:
#VLLM_API_BASE=http://localhost:8002/v1

# ====================================================================
# Service name reference (docker-compose include files)
# ====================================================================
# neo4j, qdrant, elasticsearch, opensearch, falkordb, arcadedb, memgraph,
# nebula-graphd, arangodb, apache-age, gremlin-server, hugegraph, surrealdb,
# tigergraph, chroma, milvus-standalone, weaviate, postgres-pgvector, alfresco
#
# To switch Scenario A <-> B: comment/uncomment the matching per-store line
# for each DB you use, then recreate the backend container.
